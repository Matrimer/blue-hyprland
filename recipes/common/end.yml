# Install stuff
modules:
  - type: rpm-ostree
    install:
      - hyprutils
      - hyprland-protocols-devel
      - swaybg

      # Greeter      
      - greetd
      - tuigreet
      - gtkgreet

      # For sysshell
      - gtk4-layer-shell
      - gtkmm4.0
      # For fabric:      
      - gtk3-devel
      - cairo-devel
      - gtk-layer-shell
      # - libgirepository (not found)
      - gobject-introspection-devel
      - gobject-introspection
      - python
      - python-devel
      - python3-gobject-devel
      - python3-cairo-devel
      - python-pip
      # - python-gobject
      # - python-cairo
      # - python-loguru (not found)
      - pkgconf

      # Launcher
      #- wofi # default is rofi-wayland
      # Necessary nice-to have
      - gnome-software
      # Settings
      - gnome-control-center
      - gnome-control-center-filesystem

      # For bauh
      - aria2 # multi-threaded downloads
      # - libappindicator-gtk3 # For tray-mode
      # - axel # multi-threaded downloads alternative
      # - sqlite3 # Appimage support
      # - python3-beautifulsoup4 # Web applications support
      # To run bauh natively
      # - python3-lxml # Web application support
      # General dependencies
      # - python3-yamlloader
      # - python3-dateutil
      # - python3-pyqt5-sip
      # - python3-packaging
      # - python3-requests
      
      - gparted

      # Terminal emulator
      - foot
      - foot-terminfo

      # Android
      #- waydroid is installed through script
      - lzip # needed for waydroid script. Maybe better to install in a temporary container
      # Also needs python3, which is allready available.

      # Editor
      - helix
          
      # Powersaving 
      - powertop # not in homebrew
      # - tlp
    # remove:
      # - power-profiles-daemon
      # - gnome-disk-utility
  - type: default-flatpaks
    notify: true # Send notification after install/uninstall is finished (true/false)
    system:
      # If no repo information is specified, Flathub will be used by default
      # repo-url: https://dl.flathub.org/repo/flathub.flatpakrepo
      # repo-name: flathub
      # repo-title: "Flathub (system-wide)" # Optional; this sets the remote's user-facing name in graphical frontends like GNOME Software
      install:
        - org.mozilla.firefox
        # - Flatseal: com.github.tchx84.Flatseal
        #- com.github.d4nj1.tlpui
        # - org.gnome.Loupe
      # Flatpak runtimes are not supported (like org.winehq.Wine//stable-23.08),
      # only normal flatpak applications are (like Floorp Lightning web browser in this example).
      # Installing different branches of the same Flatpak is not supported.
  #     remove:
  #     # - org.gnome.eog
  - type: brew
    nofile-limits: true
    brew-analytics: false
    auto-update: true # brew binary
    update-interval: 1d
    auto-upgrade: true # brew packages
    upgrade-interval: 1d
  
  # - type: chezmoi
  #   repository:
  #   branch:
  #   all-users: false
  #   disable-update: true
   
  - type: justfiles
    validate: false

#  - type: yafti
# Doesn't seem to launch properly.
# Has loads of python dependencies.
# It's otherwise nice, but I might make my own instead.

       
  # - type: script
    # snippets:
      # - "systemctl enable setcaps.service"
  #   #   - "setcap 'CAP_SYS_NICE=eip' /usr/bin/gamescope"
  #   scripts:
  #     - setcaps.sh
  - type: initramfs

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
